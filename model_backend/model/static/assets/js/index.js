import React from 'react';
import ReactDOM from 'react-dom';

document.addEventListener('DOMContentLoaded', () => {
  console.log("hello");
  const root = document.getElementById('root');
  ReactDOM.render(<h1>Pickles</h1>, root);
});
